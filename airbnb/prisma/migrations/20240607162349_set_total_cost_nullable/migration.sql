BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Bookings] ALTER COLUMN [total_cost] FLOAT(53) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
